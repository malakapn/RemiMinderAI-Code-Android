name: RemiMinder
description: A healthcare companion app for patients and caregivers
version: 1.3.0+86
environment:
  sdk: '>=3.0.0 <4.0.0'
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  # State Management
  flutter_riverpod: ^3.1.0
  # Navigation
  go_router: ^17.0.1
  # Networking & Authentication
  http: ^1.1.0
  url_launcher: ^6.3.1
  google_sign_in: ^6.2.1
  flutter_dotenv: ^6.0.0
  # Firebase (initialized but not used for auth yet)
  firebase_core: ^4.0.0
  firebase_auth: ^6.0.0
  # ^16.x required with firebase_core ^4 + cloud_firestore ^6 (platform_interface ^6)
  firebase_messaging: ^16.1.0
  cloud_firestore: ^6.0.0
  # Secure Storage (for JWT tokens)
  flutter_secure_storage: ^10.0.0
  # Local Storage (for app preferences)
  shared_preferences: ^2.2.2
  # Utils
  intl: ^0.20.2
  table_calendar: ^3.1.3
  # Localization
  flutter_localizations:
    sdk: flutter
  flutter_svg: ^2.0.9
  jwt_decoder: ^2.0.1
  equatable: ^2.0.5
  crypto: ^3.0.3
  uuid: ^4.2.1
  encrypt: ^5.0.1
  # Charts
  fl_chart: ^1.1.1
  # Local notifications (reminders when app is closed)
  flutter_local_notifications: ^18.0.0
  timezone: ^0.9.4
  flutter_timezone: ^3.0.0
  # Permissions & Media
  permission_handler: ^12.0.1
  camera: 0.10.6
  record: 6.1.2
  speech_to_text: ^7.3.0
  image_picker: ^1.1.1
  path_provider: ^2.1.2
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.4
flutter:
  uses-material-design: true
  generate: true
  assets:
    - assets/images/
    - assets/icon.png
    - .env
    - assets/images/google_logo.svg
    - assets/images/patient_icon.svg
    - assets/images/caregiver_icon.svg
  fonts:
    - family: Merriweather
      fonts:
        - asset: assets/fonts/Merriweather-Bold.ttf
          weight: 700
    - family: Inter
      fonts:
        - asset: assets/fonts/Inter_28pt-Regular.ttf
        - asset: assets/fonts/Inter_28pt-Medium.ttf
          weight: 500
        - asset: assets/fonts/Inter_28pt-SemiBold.ttf
          weight: 600
        - asset: assets/fonts/Inter_28pt-Bold.ttf
          weight: 700
    - family: Poppins
      fonts:
        - asset: assets/fonts/Poppins-Regular.ttf
# App Icon Configuration
flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/icon.png"
  min_sdk_android: 21
  web:
    generate: true
    image_path: "assets/icon.png"
    background_color: "#F8F4E8"
    theme_color: "#1B4E59"
dependency_overrides:
  camera_android_camerax: 0.6.18+1

<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Shown while the Flutter engine starts; no logo — solid app cream only. -->
    <style name="LaunchTheme" parent="@android:style/Theme.NoTitleBar">
        <item name="android:windowBackground">@color/background</item>
        <item name="android:windowFullscreen">false</item>
    </style>
    <!-- Window behind Flutter UI; keep cream so there is no white flash after launch. -->
    <style name="NormalTheme" parent="@android:style/Theme.NoTitleBar">
        <item name="android:windowBackground">@color/background</item>
    </style>
</resources>
